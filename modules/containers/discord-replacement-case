{ config, pkgs, ... }: {
  virtualisation.oci-containers.containers.replacement-case-bot- = {
    autoStart = true;
    image = "nonchris/discord-replacement-case";
    extraOptions = [ "--env-file=/docker/replacement-case/envfile" ];
    volumes = [ "/docker/replacement-case/data:/app/data:rw" ];
  };
}
